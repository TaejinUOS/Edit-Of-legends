# EditOfLegends — 롤 자동 컷 편집 플러그인 PRD

문서 버전: 0.1 · 작성일: 2026-09-17 · 상태: 개발 착수용 초안

## 1. 제품 정의

EditOfLegends는 리그 오브 레전드 플레이 녹화본에서 플레이어의 킬·데스·어시스트 발생 구간을 찾아, Adobe Premiere Pro에 편집 가능한 하이라이트 시퀀스를 만드는 플러그인이다.

사용자는 녹화 클립을 선택하고 앞뒤 보존 시간을 설정한 뒤 분석을 실행한다. 플러그인은 이벤트를 탐지하고 컷 후보를 제시하며, 사용자가 검토한 결과를 유형별 트랙에 배치한다. 제품의 핵심 가치는 반복적인 장면 탐색과 1차 컷 편집 시간을 줄이는 것이다. 자막·연출·최종 스토리 구성은 편집자가 이어서 수행한다.

본 문서에서 2절은 참고 영상에서 확인한 내용이다. 3절 이후의 기본값, 지원 범위, 품질 목표, 기술 구조와 일정은 **이 프로젝트를 위한 제안**이며, 참고 프로그램의 내부 구현이나 검증된 성능을 뜻하지 않는다.

## 2. 참고 영상에서 확인한 기능

출처: 허블렛 Hubblet, [「ai로 롤 자동 편집프로그램 만들어버렸습니다...」](https://www.youtube.com/watch?v=iLMUDN4cIcU). 영상 페이지의 스크립트와 1:26 시연 화면을 확인했다. 자동 스크립트에는 오인식이 있어 수치와 용어는 화면 및 문맥과 대조했다.

| 시점 | 확인한 내용 | 제품에 반영할 요구사항 |
|---|---|---|
| [1:02–1:18](https://www.youtube.com/watch?v=iLMUDN4cIcU&t=62s) | 선택한 녹화 클립의 K/D/A 및 발생 시점을 분석 | 선택 클립 분석, 이벤트 목록 |
| [1:18–1:42](https://www.youtube.com/watch?v=iLMUDN4cIcU&t=78s) | 이벤트 앞뒤 보존 시간, 동시 판정, 킬 > 어시스트 > 데스 우선순위 | 구간 설정 및 중복 처리 |
| [1:42–2:25](https://www.youtube.com/watch?v=iLMUDN4cIcU&t=102s) | 킬·어시스트·데스를 1·2·3번 트랙으로 분리하고 수동으로 선별 | 유형별 트랙 배치, 후속 편집 |
| [3:07](https://www.youtube.com/watch?v=iLMUDN4cIcU&t=187s) | 인트로·아웃트로·자막은 추가 작업 | 자동 컷 편집 중심의 MVP |
| [4:12–4:41](https://www.youtube.com/watch?v=iLMUDN4cIcU&t=252s) | CPU 코어 수 선택, 인접 이벤트 병합 설정 | 자원 설정, 이벤트 그룹화 |
| [4:41–4:58](https://www.youtube.com/watch?v=iLMUDN4cIcU&t=281s) | Python 기반 처리 및 Premiere 패널 형태 설명 | 패널과 로컬 분석 엔진 분리 검토 |

1:26 화면에는 Before 11초, After 7초, 동시 판정 15초, 분석 코어 8이 표시된다. 이는 시연 설정이며 프로그램의 기본값인지는 확인되지 않았다. 제작자는 8코어 환경에서 영상 길이의 약 1/10 수준의 처리 시간을 설명하지만, 독립적으로 측정된 벤치마크는 아니다.

영상만으로는 OCR 알고리즘, 플러그인 프레임워크, 오디오 처리, 지원 해상도, 정확도 및 실패 복구 방식을 확인할 수 없다.

## 3. 대상 사용자와 해결할 문제

주 사용자는 Premiere Pro로 본인의 롤 플레이 녹화본을 편집하는 1인 유튜버와 롤 전문 편집자다. 부 사용자는 교전 장면을 먼저 추출한 뒤 쇼츠로 재편집하는 제작자다.

현재 작업에서는 긴 녹화본을 훑으며 이벤트를 찾고, 교전 전후를 잘라내며, 중복 구간을 정리하고, 필요 없는 데스 장면을 제외하는 작업이 반복된다. 자동화 이후에는 편집자가 후보 장면의 선택과 맥락 보정에 집중할 수 있어야 한다.

대표 사용자 스토리:

- 녹화본 전체를 다시 보지 않고 내 킬·어시스트·데스 장면을 찾고 싶다.
- 교전 시작이 잘리지 않도록 이벤트 앞뒤의 보존 시간을 조절하고 싶다.
- 연속 교전은 한 장면으로 보고, 데스만 있는 장면은 쉽게 제외하고 싶다.
- 결과를 Premiere 타임라인에서 바로 재편집하고 싶다.
- 잘못 감지한 이벤트를 수정해도 원본 분석 전체를 반복하고 싶지 않다.

## 4. 성공 기준

아래 수치는 출시 전 검증할 목표다. 성능 수치는 기준 PC의 정확한 CPU 모델·메모리·저장장치·앱 버전을 고정한 후 확정한다.

| 지표 | MVP 목표 | 측정 방법 |
|---|---|---|
| 1차 컷 편집 소요 시간 | 수동 대비 중앙값 70% 이상 단축 | 동일한 장면 선별 기준으로 수동 작업과 분석·검토·생성 포함 작업 비교 |
| K/D/A 탐지 정확도 | 유형별 precision 95% 이상, recall 90% 이상 | 정답 라벨과 유형·시점 ±1초 이내 일대일 매칭 |
| 분석 속도 | 30분 1080p60 녹화본을 5분 이내 분석 | 제안 기준: 8코어 CPU, RAM 16GB, SSD; 최초 분석 5회 중앙값 |
| 타임라인 생성 | 100개 구간을 30초 이내 배치 | 분석 완료 후 생성 명령부터 완료까지 |
| 편집 정확성 | 컷 경계 오차 1프레임 이내, 추가 A/V 오차 1프레임 이내 | 분석 이벤트 시점 오차와 별도로 검증 |
| 데이터 보존 | 원본 미디어 및 기존 시퀀스 변경 0건 | 정상·취소·실패 시나리오 검증 |
| 설치 및 첫 사용 | 테스트 사용자 5명 중 4명 이상이 개발자 도움 없이 첫 결과 생성 | 설치부터 시퀀스 생성까지 관찰 |

오탐·미탐은 클립 수가 아니라 이벤트 단위로 계산한다. 여러 이벤트가 하나로 병합되더라도 원본 이벤트는 모두 평가한다. HUD 가림 등 판독 불가능 구간은 별도 집계하고 전체 영상 중 분석 가능 비율도 보고한다.

## 5. MVP 범위

### 포함

- Windows 11, 한국어 UI, Premiere Pro 패널.
- 한 경기·한 시점의 일반 플레이 녹화본, 한 번에 영상 클립 1개.
- 1920×1080 및 2560×1440, 16:9, 고정 프레임레이트 30/60fps.
- MP4 컨테이너의 H.264 영상 및 AAC 오디오를 우선 지원.
- 기본 HUD에서 본인 K/D/A 판독, HUD 영역 수동 지정 및 저장.
- 본인 점멸 준비 상태에서 쿨다운으로의 전환 탐지. F(오른쪽) 기본값, D(왼쪽) 선택 및 영역 조정. 상대 점멸은 제외.
- K/D/A 이벤트 탐지·검토, 앞뒤 구간 설정, 그룹화, 유형 선택.
- 유형별 트랙을 사용하는 새 시퀀스 생성과 동기화된 소스 오디오 보존.
- 진행 상태, 취소, 분석 결과 캐시, 실패 시 재시도.
- Python을 별도 설치하지 않아도 되는 로컬 분석 엔진 배포를 목표로 한다.

### MVP 제외

- 관전·대회 방송·플레이어 시점 전환 영상, 여러 경기가 이어진 방송 전체 자동 분리.
- `.rofl` 리플레이 직접 처리, 실시간 녹화, 게임 클라이언트 연동.
- 속도 변경·역재생·중첩 시퀀스·멀티캠·복합 편집 클립 분석.
- 자동 자막, BGM·효과음·줌·밈 삽입, 재미도 판단, 킬 없는 명장면 탐지.
- 세로 리프레이밍, 쇼츠 완성본 자동 제작, 자동 업로드.
- macOS, 다른 편집 프로그램, 클라우드 분석, 계정·결제.
- VFR·4K·HEVC의 정확성 보장 및 자동 변환. 해당 입력은 명확히 안내하고 분석을 차단한다.

초기 정식 지원 Premiere 버전은 기술 검증 단계에서 정확한 버전 번호로 고정한다. 모든 버전에 대한 호환성을 약속하지 않는다.

## 6. 핵심 사용자 흐름

1. 사용자가 Premiere 타임라인에서 원본 영상 클립 하나를 선택한다.
2. 패널에서 `선택 클립 불러오기`를 누르면 파일명, 소스 In/Out, 길이, 해상도, FPS, 오디오 구성이 표시된다.
3. K/D/A 판독 영역을 미리 보고, 필요하면 영역을 조정한다.
4. 이벤트 유형, 앞뒤 보존 시간, 동시 판정 시간, 분석 자원을 설정한다.
5. `분석 시작`을 누르면 진행률과 현재 단계를 확인할 수 있다.
6. 이벤트와 컷 후보를 검토하고, 오탐 제외·시점 수정·누락 이벤트 추가를 수행한다.
7. `새 시퀀스 생성`을 누르면 시간순으로 컷이 이어지고 유형별 트랙에 배치된다.
8. 사용자는 Premiere에서 컷 길이를 더 다듬고 자막·연출·최종 내보내기를 수행한다.

기본 설정으로 바로 진행할 수 있어야 하며, 낮은 신뢰도의 결과가 있을 때만 검토 필요 표시를 강조한다.

## 7. 기능 요구사항

P0는 MVP 출시 필수, P1은 후속 개선이다.

| ID | 우선순위 | 요구사항 | 수용 기준 |
|---|---|---|---|
| FR-01 | P0 | 선택 클립 및 원본 범위 확보 | 소스 In/Out을 정확히 읽고 K/D/A 이벤트는 그 범위만 분석한다. 시계 보정은 인접한 원본 프레임을 읽을 수 있다. 다중 선택·오프라인 미디어·미지원 입력은 이유를 표시한다. |
| FR-02 | P0 | HUD 판독 영역 설정 | 프레임 위에서 영역을 조절하고 판독한 K/D/A를 미리 확인한다. 해상도별 정규화 좌표를 저장한다. |
| FR-03 | P0 | 본인 K/D/A 변화 탐지 | K·D·A 증가분을 유형·소스 시각·변화 전후 값·신뢰 상태와 함께 기록한다. 팀 전체 킬 수는 사용하지 않는다. |
| FR-04 | P0 | 이벤트 목록 및 합계 | K/D/A 순서로 합계를 표시하고 탐지 수와 생성 클립 수를 구분한다. 각 이벤트에서 소스 시점으로 이동할 수 있다. |
| FR-05 | P0 | 컷 길이와 유형 필터 | 킬·어시스트·데스·점멸을 개별 선택하고 앞뒤 시간을 바꿀 수 있다. 설정 변경은 기존 이벤트로 즉시 재계산한다. |
| FR-06 | P0 | 동시 판정과 겹침 병합 | 8절 규칙대로 동작하며 동일 소스 프레임이 결과에 중복 삽입되지 않는다. |
| FR-07 | P0 | 수동 검토 및 보정 | 이벤트 제외·추가·유형/시각 수정, 컷 In/Out 조정이 가능하고 변경 사항을 저장한다. |
| FR-08 | P0 | 새 시퀀스 생성 | 원본 순서 및 오디오 동기를 유지하고 킬 V1·어시스트 V2·데스 V3·점멸 V4에 배치한다. 기존 시퀀스는 변경하지 않는다. |
| FR-09 | P0 | 진행률과 취소 | 읽기·분석·구간 계산·시퀀스 생성 단계를 구분한다. 취소 후 부분 결과를 완성 결과로 표시하지 않는다. |
| FR-10 | P0 | CPU 자원 설정 | 자동/1/2/4/8 워커를 제공하되 사용 가능한 CPU 수를 넘지 않는다. 자동 모드는 편집기 반응성을 고려해 산정한다. |
| FR-11 | P0 | 캐시 및 재실행 | 동일 입력과 분석 설정이면 캐시를 사용한다. HUD/엔진/소스 변경 시 무효화하고 컷 길이 변경만으로는 재분석하지 않는다. |
| FR-12 | P0 | 설치·엔진 상태 진단 | 패널에 엔진 연결 상태와 버전을 표시하고 불일치·연결 실패 시 해결 방법을 제공한다. |
| FR-13 | P1 | 사용자 프리셋 | 녹화 환경별 HUD 영역과 편집 설정을 저장·불러오기 한다. |
| FR-14 | P1 | 일괄 처리 | 여러 원본을 순차 분석하고 원본별 시퀀스를 생성한다. |
| FR-15 | P1 | 선택 장면 내보내기 | 개별 클립 출력 및 쇼츠 제작용 구간 전달을 지원한다. |
| FR-16 | P0 | 본인 점멸 사용 탐지 | 준비 상태 확인 후 연속되는 쿨다운 감소로 사용을 확정한다. 최초 쿨다운 상태·사망 아이콘은 사용으로 추정하지 않는다. 판독 공백 이후 후보는 검토 대상으로 기본 제외한다. |

## 8. 이벤트 및 컷 편집 규칙

### 8.1 K/D/A 판독

- 화면의 본인 K/D/A 숫자를 시간순으로 읽는다. 최초로 안정적으로 읽힌 값은 기준값이며 그 수만큼 이벤트를 생성하지 않는다.
- 숫자의 증가가 후속 프레임에서도 유지될 때 이벤트로 확정한다. 이벤트 시점은 안정화 완료 시점이 아니라 증가가 처음 나타난 시점으로 정한다.
- 한 프레임의 이상값, 숫자 감소, 갑작스러운 큰 변화는 재판독한다. 미확정 값으로 자동 컷을 만들지 않는다.
- HUD가 가려진 동안은 판독 불가로 표시한다. 가림 전후 수치가 증가했으나 시점을 알 수 없으면 범위를 가진 검토 후보로 남긴다.
- K가 한 번에 2 증가하면 증가량 2를 보존한다. 개별 발생 시각을 복원하지 못하면 임의의 두 시각을 만들어내지 않는다.
- 한 경기 중 카운터가 지속적으로 초기화되면 다른 경기 또는 화면 전환 가능성을 표시하고 그 이후 자동 확정을 중단한다.

### 8.2 기본 설정 제안

| 설정 | 초기값 | 허용 범위 |
|---|---|---|
| 포함 이벤트 | 킬·어시스트·데스·점멸 모두 | 0개 이상 선택; 모두 끄면 인게임 0:50–3:30 클립만 생성 |
| 이벤트 이전 보존 | 11초 | 0–120초, 0.1초 단위 |
| 이벤트 이후 보존 | 7초 | 0–120초, 0.1초 단위 |
| 동시 판정 시간 | 15초 | 0–60초, 0.1초 단위 |
| 대표 유형 우선순위 | 킬 > 어시스트 > 데스 > 점멸 | MVP 고정, 화면에 안내 |
| 분석 자원 | 자동 | 자동/1/2/4/8 워커 |

앞뒤 시간이 모두 0이면 생성을 허용하지 않는다. 위 초기값은 참고 시연을 출발점으로 정한 제안이며 사용자 테스트로 조정한다.

### 8.3 필터·그룹화·병합 순서

1. 인게임 시계 HUD를 읽어 게임 시간 0:50–3:30에 대응하는 소스 구간을 찾고 V1 오프닝 클립을 첫 번째로 만든다. 선택한 원본 범위 안의 부분만 사용한다. 이벤트 여부와 유형 필터에 영향받지 않으며 수동 트림할 수 없다.
2. 제외 처리한 이벤트, 선택하지 않은 유형, 오프닝에 포함된 이벤트를 후속 컷 생성 대상에서 제거한다. 원본 탐지 목록은 보존한다.
3. 남은 이벤트를 소스 시각 순으로 정렬한다. 인접 이벤트 간 간격이 동시 판정 시간 이하이면 하나의 그룹으로 연결한다. 0초 설정은 정확히 같은 시각만 그룹화한다.
4. 그룹 시작은 `첫 이벤트 - Before`, 끝은 `마지막 이벤트 + After`로 정한다. 그룹 내 모든 이벤트의 메타데이터를 보존한다.
5. 구간을 오프닝 끝과 선택 클립의 소스 Out 안으로 제한한다. 음수 시각이나 원본 끝 초과 구간을 만들지 않는다.
6. 보존 구간이 겹치거나 맞닿으면 유형과 관계없이 합집합으로 병합한다. 이 단계는 동시 판정 시간과 별개다.
7. 최종 구간에 포함된 이벤트 중 우선순위가 가장 높은 유형으로 트랙을 결정한다. 보존 여유 구간에 우연히 등장한 제외 유형은 대표 유형을 바꾸지 않는다.
8. 구간을 소스 프레임 경계에 정렬하고, 시간순으로 빈틈 없이 새 시퀀스에 배치한다. 긴 연속 교전은 길이 경고를 표시하되 자동으로 잘라 버리지 않는다.

예: 인게임 시계와 영상 시각이 일치하고 킬 280초, 어시스트 288초, 데스 292초에 Before 11초·After 7초·동시 판정 15초이면, 결과는 오프닝 50–210초와 이벤트 269–299초의 두 클립이며 둘 다 V1에 배치한다. 세 이벤트의 기록은 모두 남는다.

데스를 필터에서 끄는 것은 데스만을 이유로 클립을 만들지 않는다는 뜻이다. 킬 장면의 보존 구간에 포함된 데스 화면까지 제거하는 기능은 아니다.

### 8.4 타임라인과 오디오

- 새 시퀀스명: `EOL_<원본명>_<생성시각>`. 이름 충돌 시 고유 접미사를 붙인다.
- 유형 트랙: V1=오프닝·킬, V2=어시스트, V3=데스, V4=점멸. 각 컷은 공통 출력 시간축에서 이전 컷 바로 뒤에 시작한다. 트랙마다 0초부터 독립적으로 쌓지 않는다.
- 원본 오디오가 N개 트랙이면 A1…AN에 같은 In/Out으로 배치하고 영상과 연결한다. 이벤트 유형 때문에 오디오를 중복 복사하지 않는다.
- 원본 해상도·FPS를 기본으로 사용하고, 무음 원본도 지원한다. 믹싱·음량 보정은 하지 않는다.
- 원본 미디어를 참조하는 비파괴 편집으로 구현한다. 컷마다 새 동영상 파일을 렌더링하지 않는다.
- MVP는 선택 클립의 소스 구간과 소스 오디오를 사용한다. 원본 시퀀스에 적용한 이펙트·자막·주변 오디오의 복제는 범위에 포함하지 않으며 생성 전에 이를 안내한다.
- 재실행할 때 기존 생성 시퀀스를 덮어쓰지 않는다. 실패 시 새로 만든 미완료 결과만 식별하고 재시도할 수 있어야 한다.

## 9. 패널 구성

패널은 위에서 아래로 다음 영역을 제공한다.

| 영역 | 표시 및 조작 |
|---|---|
| 소스 | 선택 클립 불러오기, 파일명·길이·In/Out·FPS·오디오 정보 |
| 판독 확인 | HUD 영역 미리보기, K/D/A 판독값, 영역 조정 |
| 컷 설정 | 이벤트 체크박스, Before/After, 동시 판정, CPU 설정 |
| 분석 | 분석 시작·취소, 단계·진행률·예상 남은 시간 |
| 결과 | K/D/A 증가 합계, 이벤트 수, 컷 수, 결과 길이, 검토 필요 수 |
| 후보 목록 | 유형·시각·길이·신뢰 상태, 미리보기 이동, 포함 여부 및 수정 |
| 생성 | 새 시퀀스 생성, 완료 시 생성 시퀀스 열기 |

상태 전이: `입력 대기 → 입력 확인 → 분석 중 → 검토 가능 → 생성 중 → 완료`. 실패 상태에서는 실패 단계와 재시도 방법을, 취소 상태에서는 보존된 캐시 범위를 보여준다. 색상만으로 유형이나 오류를 구분하지 않는다.

## 10. 기술 방향과 선행 검증

제안 구조는 **Premiere 패널 → 로컬 분석 엔진 → 이벤트/컷 계획 → Premiere 시퀀스 생성**이다. 분석과 타임라인 조작을 분리해 분석 중 편집기의 UI가 멈추지 않게 한다.

| 구성요소 | 책임 | 후보 기술 |
|---|---|---|
| Premiere 패널 | 선택 소스 조회, 설정, 검토, 시퀀스 생성 | TypeScript 및 UXP |
| 로컬 분석 엔진 | 디코딩, HUD 판독, 시간축 검증, 이벤트 생성 | Python 패키징 실행 파일 |
| 영상 처리 | 프레임 추출, 메타데이터 조회, 영역 전처리 | FFmpeg/ffprobe, OpenCV 후보 |
| 숫자 인식 | K/D/A ROI 숫자 및 구분자 판독 | 경량 OCR 또는 숫자 분류기 비교 후 선택 |
| 저장 | 작업 상태, 분석 캐시, 사용자 보정 | 로컬 JSON 또는 SQLite |

Adobe는 Premiere용 UXP와 시퀀스 편집 API를 제공한다. UXP를 우선 검토하되 해당 API의 존재만으로 필요한 기능 전체가 검증됐다고 간주하지 않는다. [Adobe UXP 문서](https://developer.adobe.com/premiere-pro/uxp/), [SequenceEditor API](https://developer.adobe.com/premiere-pro/uxp/ppro-reference/classes/sequenceeditor)

개발 첫 단계에서 다음 항목을 실제 지원 대상 버전으로 검증한다.

1. 선택 클립의 미디어 경로, 소스 In/Out, 오디오 매핑 조회.
2. 새 시퀀스와 3개 영상 트랙 생성, 서로 다른 소스 구간 삽입, 영상·오디오 연결.
3. 생성 실패·취소·재실행 시 프로젝트 일관성.
4. 패널과 로컬 엔진의 통신, 파일 접근 권한, 엔진 설치·시작 방법.
5. 한국어·공백·긴 파일 경로와 다중 오디오 입력 처리.

필수 기능이 막히면 지원 버전 조정 또는 CEP/ExtendScript 대안을 기술 결정 기록에 남긴다. 외부 XML 가져오기만 제공하는 우회 방식은 패널에서의 직접 생성과 사용 경험이 달라지므로 별도 범위 결정이 필요하다.

MVP는 녹화 파일만으로 동작하도록 설계하며 외부 게임 데이터나 범용 LLM 호출을 필수 의존성으로 두지 않는다. OCR 방식과 샘플링 간격은 정확도·속도 실험 결과로 정한다.

## 11. 데이터와 시간축 계약

| 엔터티 | 필수 정보 |
|---|---|
| Source | 미디어 식별자·변경 지문, 파일 경로, source In/Out, duration, FPS 분자/분모, 해상도, 오디오 매핑 |
| AnalysisJob | job ID, 엔진 버전, 분석 설정, 상태, 진행 위치, 경고 및 오류 |
| Event | event ID, 유형, 소스 시각/불확실 구간, 증가량, 변화 전후 K/D/A, 신뢰 상태, 사용자 보정 여부 |
| ClipPlan | source In/Out, output In/Out, 포함 event IDs, 대표 유형, 대상 트랙, 포함 여부 |

시간의 기준은 미디어 시작점이다. 게임 내 시계, 원본 타임코드, 소스 프레임 인덱스, 시퀀스 시각을 혼용하지 않는다. 계산에는 정수 프레임 또는 유리수 시간값을 사용하고 표시 단계에서만 초로 변환한다. 구간은 `[In, Out)`으로 표현해 경계 프레임 중복을 방지한다.

캐시 키에는 소스 변경 지문·분석 범위·HUD 설정·엔진 버전·분석 설정을 포함한다. 컷 보존 시간·유형 필터·그룹화 설정은 별도 저장해 분석 없이 컷 계획을 다시 만든다.

## 12. 비기능 요구사항 및 예외 처리

- **로컬 처리:** 영상·음성·프레임을 외부 서버로 전송하지 않는다. 기본 사용에 계정을 요구하지 않는다.
- **자원 관리:** 전체 영상을 메모리에 적재하지 않는다. 워커 수 변경은 다음 분석에 적용하고 실제 설정을 기록한다.
- **통신:** 로컬 서비스 방식을 쓰면 loopback으로 제한하고 작업별 인증 및 경로 검증을 적용한다.
- **반응성:** 분석 중에도 패널이 반응해야 하며 취소 요청을 2초 내 표시하고 워커 종료는 목표 5초 내 처리한다.
- **원본 보존:** 미디어 파일과 기존 시퀀스를 수정하지 않는다. 캐시 삭제는 플러그인이 생성한 캐시만 대상으로 한다.
- **관찰 가능성:** 처리 단계·소요 시간·엔진 버전·오류 코드를 로컬 로그에 남긴다. 로그 공유는 사용자가 실행하고 파일 경로 등 식별 정보를 검토할 수 있게 한다.

| 상황 | 기대 동작 |
|---|---|
| 이벤트가 실제로 없음 | 정상 완료와 0개 결과를 표시하고 빈 시퀀스는 만들지 않음 |
| HUD를 계속 읽을 수 없음 | 0개 이벤트와 구분해 분석 불가 표시, 영역 조정 안내 |
| HUD가 잠시 가려짐 | 해당 범위를 표시하고 불확실한 증가를 검토 후보로 제공 |
| 녹화 중간부터 시작 | 최초 K/D/A를 기준값으로 설정, 이후 증가만 탐지 |
| 녹화가 이벤트 직후 종료 | 보존 구간을 소스 끝으로 제한 |
| 원본 삭제·변경·오프라인 | 분석/생성을 중단하고 재연결 또는 재분석 안내 |
| 미지원 FPS·코덱·VFR | 사전 검사에서 차단하고 지원 입력 조건 표시 |
| 엔진 오류·취소 | 완료된 캐시만 보존, 실패 단계부터 가능한 범위에서 재시도 |
| 분석 도중 선택 클립 변경 | 시작 시 확정한 소스 기준으로 결과 유지, 생성 전에 대상 정보 재확인 |
| 시퀀스 생성 중 실패 | 새 결과를 미완료로 표시, 기존 프로젝트 내용 보존, 재시도 중복 방지 |

## 13. 검증 및 출시 조건

평가 자료는 사용 권한이 있는 서로 다른 녹화본 최소 20개, 총 10시간 이상, K/D/A 이벤트 각각 100개 이상을 목표로 한다. 해상도·HUD 크기·오디오 트랙 구성을 분산하고, 개발 튜닝 자료와 최종 평가 자료를 경기 단위로 분리한다. 정답 시점은 HUD 카운터가 최초 증가한 프레임으로 정의한다.

필수 시나리오는 단일 킬, 짧은 간격의 연속 킬, 어시스트 후 데스, K/D/A 동시 증가, 겹치는 보존 구간, 유형 필터, 소스 시작/끝 이벤트, 트림된 클립, 가려진 HUD, 이벤트 없음, 다중 오디오, 한글 경로, 취소 및 생성 실패다.

출시 게이트:

1. 모든 P0 수용 기준 통과.
2. 지원 입력에서 4절의 정확도·속도·동기 목표 충족. 미달 시 실제 수치와 조정된 지원 범위를 문서화.
3. 사용자가 기존 시퀀스 손상 없이 반복 실행·취소·수정할 수 있음.
4. 확정한 Premiere 버전에서 설치부터 첫 시퀀스 생성까지 검증.
5. 베타 사용자 5명 이상이 각자 최소 3개 녹화본을 처리하고 치명적 오류가 남아 있지 않음.

## 14. 개발 단계

아래 일정은 전담 개발자 1명, 테스트 녹화본 확보, 목표 API 사용 가능을 전제로 한 6–8주 추정이다. 선행 검증 후 다시 산정한다.

| 단계 | 예상 기간 | 산출물 및 종료 기준 |
|---|---|---|
| M0: 기술 검증 | 1주 | 고정 이벤트 3개로 다중 트랙·오디오 시퀀스 생성, 패널–엔진 통신, 지원 버전 결정 |
| M1: 이벤트 분석 | 1–2주 | ROI 설정, K/D/A 탐지, 신뢰 상태 및 라벨 평가 보고서 |
| M2: 컷 계획·검토 | 1주 | 필터·그룹화·겹침 병합, 후보 검토·수정, 캐시 |
| M3: 통합 | 1–2주 | 실제 분석에서 새 시퀀스까지 연결, 시간축·오디오·취소 검증 |
| M4: 배포·베타 | 1–2주 | 설치 패키지, 성능 결과, 사용 가이드, 베타 피드백 반영 |

첫 구현 목표는 **녹화본 하나에서 K/D/A 이벤트를 추출하고, 원본과 오디오 동기를 유지한 3개 트랙의 새 시퀀스를 생성하는 전체 흐름**이다.

## 15. 주요 위험과 미확정 사항

| 항목 | 현재 제안 | 확정 시점 |
|---|---|---|
| 사용자의 실제 Premiere 버전 | UXP 우선, 한 버전부터 검증 | M0 시작 |
| 녹화 조건 | 본인 POV, 기본 HUD, 1080p/1440p CFR | 대표 녹화본 확보 시 |
| HUD 오인식 | 영역 미리보기·수동 보정·연속 프레임 검증 | M1 평가 |
| CPU 처리 성능 | 국소 영역 분석과 워커 수 조절 | M1 벤치마크 |
| 다중 오디오 및 시간축 API | 지원 버전에서 필수 검증 | M0 종료 |
| 패치에 따른 HUD 변경 | HUD 프로필·엔진 버전별 캐시 관리 | 베타 및 유지보수 |
| 배포 방식 | 초기에는 테스트용 패널과 패키징 엔진 | M3 이전 |
| 유료화·배포 채널 | MVP 검증 이후 결정 | 베타 이후 |

사용자에게 추가로 확인하면 범위가 더 정확해지는 정보는 Premiere 버전, 평소 녹화 해상도/FPS, OBS 오디오 트랙 구성, 대표 녹화본, 장편 편집과 쇼츠 중 우선 용도다. 해당 정보가 없어도 본 초안의 범위로 기술 검증을 시작할 수 있다.
